<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hangyeong.mynote">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/memo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/memo"
        android:supportsRtl="true">
        <activity android:name=".MainActivity" android:theme="@style/Theme.MyNote.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NoteActivity" android:theme="@style/Theme.AppCompat.Light"/>
        <activity android:name=".PasswordActivity" android:theme="@style/Theme.AppCompat.Dialog" android:label=" "/>
    </application>

</manifest>